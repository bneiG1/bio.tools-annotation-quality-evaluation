# Core dependencies for bio.tools analysis
requests>=2.28.0
jsonschema>=4.0.0
pandas>=1.5.0
plotly>=5.0.0
pyyaml>=6.0

# Streamlit for web interface
streamlit>=1.49.0
boltons

# Data processing
numpy>=1.21.0
openpyxl>=3.0.0

# Linter dependencies (used by biotools-linter submodule)
owlready2
rdflib

# Development dependencies (optional)
pytest>=7.0.0
